name: drama-plugin
version: 0.1.0
description: Host-agnostic historical drama domain plugin
skills_directory: skills
supported_tool_domains:
  - project
  - asset
  - history
  - generation
  - media
  - context
context_capabilities:
  scopes: [PROJECT, STORY, EPISODE, SCENE, SHOT, ASSET, HISTORICAL]
  supports_full_context: true
  supports_patch: true
