interface:
  display_name: "Cinematic Execution Projection"
  short_description: "Project approved direction into a frozen shot spec"
  default_prompt: "Use $cinematic-direction to assemble approved department originals into the existing source-bound CinematicShotSpec."
