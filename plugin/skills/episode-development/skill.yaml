code: episode-development
name: Episode Development
description: Develop one persistent Episode from the current Script.
context:
  required: [script]
  optional: [episodeId, neighboringEpisodes, researchContext, temporaryState]
  refresh_after: [episode.create_episode, episode.save_episode]
tools:
  preferred: [script.get_script, episode.get_episode, episode.list_episodes, episode.create_episode, episode.save_episode]
  allowed: [research.verify_claim, context.build_context, context.refresh_context]
completion:
  conditions:
    - The Episode has one necessary dramatic job, a character objective, opening hook, progressing conflict, meaningful turn, earned ending, and a material entry-to-exit state change rather than a mechanical Script slice.
    - It remains faithful to the Script, continuous with neighboring Episodes, and ready for downstream Scene development.
    - Persistence follows sufficient context, an internal plan, a complete draft, and a passing Episode review with no unresolved historical or continuity conflict.
