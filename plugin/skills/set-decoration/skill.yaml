code: set-decoration
name: Set Decoration
description: Deprecated set-decoration view; forward concrete visual design to specialized-asset-design.
context:
  required:
  - sourcePins
  - departmentInputs
  optional:
  - directorPackageRef
  - approvedBaseline
  - continuityRef
  - userFeedback
  refresh_after: []
tools:
  preferred:
  - work.get_work
  - scene.get_scene
  allowed:
  - script.get_script
  - episode.get_episode
  - shot.get_shot
  - asset.get_asset
  - context.build_context
completion:
  conditions:
  - Return the specialized asset reference, or UPSTREAM_INFORMATION_REQUIRED; never
    author a competing concrete asset.
