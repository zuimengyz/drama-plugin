interface:
  display_name: "shot-generation"
  short_description: "Drama domain SOP for shot-generation"
  default_prompt: "Use $shot-generation for the current drama task."

dependencies:
  tools:
    - type: "mcp"
      value: "drama-context"
      description: "External Drama SHOT context server"
      transport: "streamable_http"
      url: "http://127.0.0.1:8765/mcp"
